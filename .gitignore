# 실행 파일
*.exe
*.out
*.o

# 확장자 없는 컴파일 실행 파일 (예: a, b, main, solution, program 등)
# 이미 추적 중인 makefile, prg 같은 파일은 영향받지 않음
*
!*/
!*.*
*.json

# VSCode 설정 (팀 프로젝트면 올릴 수도 있으니 상황에 따라)
.vscode/

# 디버그 관련
*.pdb
*.ilk

# CMake 쓴다면
build/
CMakeCache.txt
CMakeFiles/

CLAUDE.md